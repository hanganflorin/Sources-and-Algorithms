#include <fstream>
#include <cstring>
using namespace std;

ifstream is("text.in");
ifstream os("text.out");

string s;

int main()
{
    is.getline(s, )
    is.close();
    os.close();
    return 0;
}
