#include <cstdio>
using namespace std;

FILE * is = fopen("jap2.in", "r");
FILE * os = fopen("jap2.out", "w");

int P, Q, a, b, f[1000000000000000001];

int main()
{
    fscanf(os, "%d%d", P, Q );
    fclose(is);
    fclose(os);
    return 0;
}
